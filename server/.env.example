# JWT Secret สำหรับเซ็น token
SECRET=your_super_secret_key

# Database URL (ถ้าใช้ Prisma/Postgres/MySQL)
DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/hotel"

# Default admin สำหรับ endpoint /register-default-admin
DEFAULT_ADMIN_EMAIL=admin@admin.com
DEFAULT_ADMIN_PASSWORD=admin1234
DEFAULT_ADMIN_SECRET=supersecret123

# (ถ้ามีการตั้งค่าอื่นๆ เช่น SMTP, 3rd party API, ฯลฯ)
# SMTP_HOST=smtp.example.com
# SMTP_USER=...
# SMTP_PASS=...